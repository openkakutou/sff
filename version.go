// Package sff reads and writes MUGEN/Ikemen GO sprite sheet (.sff) files,
// v1 and v2, and resolves sprite palettes (including external .act
// overrides). It has no rendering dependency and compiles to WebAssembly.
package sff

// Version is the library version.
const Version = "0.3.1"
